{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run Uvicorn Server",
			"type": "shell",
			"command": "uvicorn main:app --host 0.0.0.0 --port 8000"
		},
		{
			"label": "Run Uvicorn Server",
			"type": "shell",
			"command": "uvicorn main:app --host 0.0.0.0 --port 8000"
		},
		{
			"label": "Run Uvicorn Server",
			"type": "shell",
			"command": "uvicorn main:app --host 0.0.0.0 --port 8000"
		}
	]
}